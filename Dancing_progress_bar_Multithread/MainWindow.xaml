<Window x:Class="Dancing_progress_bar_Multithread.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dancing_progress_bar_Multithread"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Количество ProgressBar:" Margin="5"/>
            <TextBox x:Name="CountProgressBar" Width="50" Margin="5"/>
            <Button Content="Start" x:Name="StartButton" Margin="5" Width="50" Click="StartButton_Click"/>
            <Button Content="Stop" x:Name="StopButton" Margin="5" Width="50" Click="StopButton_Click"/>
            <Button Content="Clear" x:Name="ClearButton" Margin="5" Width="50" Click="ClearButton_Click"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="5" x:Name="ListProgressBar"/>
        </ScrollViewer>
    </Grid>
</Window>
